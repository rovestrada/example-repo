name: Dummy Workflow

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run dummy script
        run: python3 scripts/hello.py
